CREATE TABLE animals (name VARCHAR(20), kind VARCHAR(8), years INTEGER) PRIMARY KEY (name, kind);
INSERT INTO animals VALUES FROM ("Joe", "cat", 4);
INSERT INTO animals VALUES FROM ("Spot", "dog", 10);
INSERT INTO animals VALUES FROM ("Snoopy", "dog", 3);
INSERT INTO animals VALUES FROM ("Tweety", "bird", 1);
INSERT INTO animals VALUES FROM ("Joe", "bird", 2);
SHOW animals;
DELETE FROM animals WHERE (name == "Joe");
SHOW animals;
dogs <- select (kind == "dog") animals;
SHOW dogs;
old_dogs <- select (years >= 10) dogs;
SHOW old_dogs;
cats_or_dogs <- dogs + (select (kind == "cat") animals);
SHOW cats_or_dogs;
CREATE TABLE species (kind VARCHAR(10)) PRIMARY KEY (kind);
CREATE TABLE ages (kind VARCHAR(12), random INTEGER) PRIMARY KEY(kind);
SHOW species;
INSERT INTO species VALUES FROM RELATION project (kind) animals;
SHOW species;
a <- rename (aname, akind) (project (name, kind) animals);
SHOW a;
common_names <- project (aname) (select (aname == name && akind != kind) (a * animals));
answer <- common_names;
SHOW answer;
SHOW ((select (kind == "dog") animals) JOIN (species JOIN ages));
SHOW animals;
UPDATE animals SET kind = "duck", years = 27 WHERE kind == "bird";
SHOW animals;
WRITE animals;
CLOSE animals;
EXIT;
